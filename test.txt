program multMatrix:

  define pruebaGlobal as integer. 

    method void algo(integer b, integer x):
      define a as integer = 0.
      printline(a).
      return 0.
    end method

    method void main():
      define a as integer.
      define c as integer.
      algo(a, c).
    end method

end program
