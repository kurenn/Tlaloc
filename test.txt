program test:

	method void main():
  
      define var as integer.
      define var2 as integer = 5.
      define var3 as integer = 3.

      var = (2 * ((3 + 4) / 6) ^ var3 + ((6+2 * 8) + var2).
      printline(var2).
      printline(var).
            
	end method

end program
