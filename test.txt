program edo:

	method void main():
            define r as integer.
            define edo as integer = 666. 
            define _kuri as integer = 10.
            define a as integer.  
            define alguien as string.
            define algo as integer = 2.         
            edo = log(180).

        for a = 3 to 8:
            for _kuri = 2 to 9:
                printline(_kuri).
            end for
            printline(a).
            a=a+1.
		end for
        
	end method

end program
