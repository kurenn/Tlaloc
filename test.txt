program edo:

	method void main():
            define r as integer.
            define edo as integer = 666. 
            define _kuri as integer = 10.
            define a as integer.  
            define alguien as string.
            define algo as integer.         
            edo = log(180).
        
            r = 5 - 10.            
            printline(r).
        for a = 1 to 10 step 1:
            for r = 1 to 10 step 2:
                for _kuri = 1 to 10 step 3:
                    print(_kuri).
		        end for
            printline("").
            print(": for2 :" + r).
            end for
        printline(": for1 : " + a).
		end for
        
	end method

end program
