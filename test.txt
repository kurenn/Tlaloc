program edo:

	method void main():
        define arr as integer [10].
        define a as integer = 0.

        while (a < 10):
            arr[a] = a.
            printline(arr[a]).
            a = a + 1.
        end while
        printline("done!").
        
	end method

end program
